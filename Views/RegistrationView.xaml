<Window x:Class="WaybillWpf.Views.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels1="clr-namespace:WaybillWpf.ViewModels"
        mc:Ignorable="d"
        Title="Регистрация нового пользователя" 
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height" Width="350"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Ideal"
        d:DataContext="{d:DesignInstance viewModels1:RegistrationViewModel, IsDesignTimeCreatable=False}">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Логин:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="0" Name="UsernameTextBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,20,0,5" />

        <TextBlock Grid.Row="1" Text="Пароль:" Margin="0,5,0,5"/>
        <PasswordBox Grid.Row="1" Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,25,0,5"/>

        <TextBlock Grid.Row="2" Text="Повторите пароль:" Margin="0,5,0,5"/>
        <PasswordBox Grid.Row="2" Name="ConfirmPasswordBox" PasswordChanged="ConfirmPasswordBox_PasswordChanged" Margin="0,25,0,5"/>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10"
                   TextWrapping="Wrap" MinHeight="30"/>

        <StackPanel Grid.Row="4" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Зарегистрироваться" 
                    Background="White"
                    Command="{Binding RegisterCommand}" 
                    IsDefault="True" 
                    MinWidth="120" Margin="5" Padding="5"/>
            <Button Content="Войти" 
                    Command="{Binding ToLoginCommand}" 
                    Background="White"
                    MinWidth="80" Margin="5" Padding="5"
                    BorderThickness="0"/>
        </StackPanel>
        
    </Grid>
</Window>