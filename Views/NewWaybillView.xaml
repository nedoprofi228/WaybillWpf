<Window x:Class="WaybillWpf.Views.NewWaybillView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WaybillWpf.Views"
        xmlns:viewModels="clr-namespace:WaybillWpf.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:NewWaybillViewModel, IsDesignTimeCreatable=False}"
        Title="Создание нового путевого листа"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height" Width="400">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Доступные машины:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="0" Margin="0,20,0,5"
                  ItemsSource="{Binding AvailableCars}"
                  SelectedItem="{Binding SelectedCar}"
                  DisplayMemberPath="Model"
                  IsEnabled="{Binding !IsLoading}"/>

        <TextBlock Grid.Row="1" Text="Доступные водители:" Margin="0,10,0,5"/>
        <ComboBox Grid.Row="1" Margin="0,30,0,5"
                  ItemsSource="{Binding AvailableDrivers}"
                  SelectedItem="{Binding SelectedDriver}"
                  DisplayMemberPath="DriverName"
                  IsEnabled="{Binding !IsLoading}"/>
        
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" 
                   Margin="0,10" MinHeight="30" TextWrapping="Wrap"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Создать" 
                    Command="{Binding CreateCommand}" 
                    IsDefault="True" 
                    MinWidth="80" Margin="5" Padding="5"/>
            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}" 
                    IsCancel="True" 
                    MinWidth="80" Margin="5" Padding="5"/>
        </StackPanel>
        
        <Border Grid.RowSpan="4" Background="#7F000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Загрузка доступных..." HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Foreground="White" FontSize="16"/>
        </Border>
    </Grid>
</Window>
