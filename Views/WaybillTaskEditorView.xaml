<Window x:Class="WaybillWpf.Views.WaybillTaskEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WaybillWpf.Views"
        mc:Ignorable="d"
        Title="Задание водителю" Height="400" Width="350"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Дата исполнения (гр. 21)" Margin="0,5"/>
            <DatePicker SelectedDate="{Binding Date}"/>

            <TextBlock Text="Откуда (гр. 22)" Margin="0,5"/>
            <TextBox Text="{Binding DeparturePoint}"/>

            <TextBlock Text="Куда (гр. 23)" Margin="0,5"/>
            <TextBox Text="{Binding ArrivalPoint}"/>

            <TextBlock Text="Пробег, км (гр. 24)" Margin="0,5"/>
            <TextBox Text="{Binding Mileage}"/>

            <TextBlock Text="Заказчик (гр. 25)" Margin="0,5"/>
            <TextBox Text="{Binding CustomerName}"/>

            <TextBlock Text="Прочие сведения (гр. 26)" Margin="0,5"/>
            <TextBox Text="{Binding OtherInfo}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="100" Margin="5"/>
                <Button Content="Отмена" IsCancel="True" Width="80" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>