<UserControl x:Class="WaybillWpf.Views.Admin.AdminCarsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:WaybillWpf.ViewModels.Admin"
             d:DataContext="{d:DesignInstance Type=viewmodels:AdminCarsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Панель кнопок -->
            <RowDefinition Height="*"/>    <!-- Таблица -->
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Парк автомобилей" 
                       FontSize="20" FontWeight="Bold" 
                       VerticalAlignment="Center" Margin="0,0,20,0"/>
            
            <Button Content="Добавить" Command="{Binding AddCarCommand}" Width="100" Margin="5"/>
            <Button Content="Редактировать" Command="{Binding EditCarCommand}" Width="100" Margin="5"/>
            <Button Content="Удалить" Command="{Binding DeleteCarCommand}" Width="100" Margin="5"/>
            
            <!-- Кнопка обновления справа -->
            <Button Content="Обновить" Command="{Binding LoadDataCommand}" Width="80" Margin="20,5,5,5"/>
        </StackPanel>

        <!-- Таблица машин -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Cars}" 
                  SelectedItem="{Binding SelectedCar}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            
            <DataGrid.Columns>
                
                <!-- Модель -->
                <DataGridTextColumn Header="Модель / Гос. номер" Binding="{Binding Model}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="5,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- Расход топлива -->
                <!-- StringFormat=F2 показывает 2 знака после запятой -->
                <DataGridTextColumn Header="Норма расхода (л/100км)" 
                                    Binding="{Binding FuelRate, StringFormat=F2}" 
                                    Width="180"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>