<Window x:Class="WaybillWpf.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WaybillWpf.Views"
        mc:Ignorable="d"
        Title="Вход в систему" 
        WindowStartupLocation="CenterScreen"
        SizeToContent="Height" Width="350"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Ideal">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Логин:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="0" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,20,0,5" />

        <TextBlock Grid.Row="1" Text="Пароль:" Margin="0,5,0,5"/>
        <PasswordBox Grid.Row="1" Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,25,0,5"/>

        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" 
                   Margin="0,10" MinHeight="30" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Войти" 
                    Background="White"
                    Command="{Binding LoginCommand}" 
                    IsDefault="True" 
                    MinWidth="80" Margin="5" Padding="5"/>
            
            <Button Content="Регистрация" 
                    Command="{Binding OpenRegistrationCommand}" 
                    Background="White"
                    IsCancel="False" 
                    MinWidth="80" Margin="5" Padding="5"
                    BorderThickness="0"/>
        </StackPanel>
        
    </Grid>
</Window>
